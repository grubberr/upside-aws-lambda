service: lambda-wikipedia-search

frameworkVersion: '3'

provider:
  name: aws
  runtime: python3.9

functions:
  lambda_handler:
    handler: handler.lambda_handler

plugins:
  - serverless-python-requirements
